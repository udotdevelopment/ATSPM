@model SPM.Models.MetricResultViewModel

<script type="text/javascript">
    function ShowMetricUrl() {
    @Html.Raw(Model.ShowMetricUrlJavascript)
}</script>

@{
    foreach(String s in Model.ChartPaths)
    {
        <img src="@s" alt="Chart" />
    }
}
